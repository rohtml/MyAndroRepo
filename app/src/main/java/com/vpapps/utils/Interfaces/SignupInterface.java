/*
package com.vpapps.utils.Interfaces;

import com.synnex.chandankanya.Model.CommonResponceModel;
import com.synnex.chandankanya.Utils.Const;

import retrofit2.Call;
import retrofit2.http.Field;
import retrofit2.http.FormUrlEncoded;
import retrofit2.http.POST;

public interface SignupInterface {
    @POST(Const.Register)
    @FormUrlEncoded
    Call<CommonResponceModel> doLogin(@Field("mobile") String mobile,
                                      @Field("email") String email,
                                      @Field("password") String password,
                                      @Field("name") String name,
                                      @Field("state") String state,
                                      @Field("dist") String dist,
                                      @Field("taluka") String taluka,
                                      @Field("village") String village

    );
}
*/
